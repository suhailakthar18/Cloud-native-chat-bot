# In this Clone the repository come to docker folder and build the image and run it 
**APPROCH 1**
  make sure your llm is running on host and use the command
  docker run -d -e BOT_TOKEN=<Replace-Your-Token>
**APPROCH 2**
  Run the docker image for ollama and exec the containe and say ollama pull llama2
**Approch 3**
  Simply say docker-compose-up
